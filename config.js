/**
 * Site URL configuration used throughout the application.
 * Loaded from environment variables for different environments (development, production, etc.).
 * This value is exposed to the browser via the NEXT_PUBLIC_ prefix.
 */
export const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL;